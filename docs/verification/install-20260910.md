# DataHub 本機安裝驗證 — 2026-09-10

## 範圍與結果

完成固定版官方 DataHub 本機開發環境的 clone、部署、CLI 安裝與 HTTP/API 冒煙測試。不是完整產品／架構 G0 驗收，也不是從上游原始碼編譯。

- 上游：`datahub-project/datahub`，tag `v1.7.0.1`，commit `e99431ec510d7a2001f815c6bf70913c493af76e`；submodule 工作區未修改。
- Core／Frontend／Actions／Upgrade 與三個底層服務全部依 `deploy/images.lock.yaml` 固定 digest。
- CLI：`acryl-datahub==1.7.0.9`，Python 3.11；61 個 Python 套件版本記於 `deploy/cli.lock.txt`。
- Compose project：`ekop-datahub`；UI `127.0.0.1:9002`，GMS `127.0.0.1:18080`。
- 六個常駐容器 running；五個有 healthcheck 的容器 healthy。Actions 無 healthcheck，另確認程序 running、executor 初始化日誌無 ERROR／401／403。
- `system-update` 一次性初始化工作成功退出（exit 0）。

## 直接驗證

| 檢查 | 結果 |
| --- | --- |
| Compose 原生配置解析、全部映像固定 digest | PASS |
| 僅兩個 loopback 發布埠、獨立資料卷／網路 | PASS |
| 不掛載主機 AWS 憑證或 Docker socket | PASS |
| GMS／Frontend 都啟用 Metadata Service Authentication | PASS |
| 隨機本機憑證、重跑初始化不改寫、私人目錄權限 | PASS |
| UI `/login`、Frontend `/health`、GMS `/health` | HTTP 200 |
| 未登入請求直連 GMS 與 Frontend GraphQL | 拒絕 |
| 官方預設帳密登入 | 拒絕 |
| 使用本機隨機密碼登入，session 呼叫 GraphQL `me` | PASS |
| 僅重啟本案 GMS／Frontend，再次執行上述 live checks | PASS |
| 既有服務未重啟，其既有埠仍監聽 | PASS |

可重跑檢查：

```bash
python3 tests/test_installation.py
python3 tests/test_installation.py --live
```

機械／認證冒煙為本輪必要 gate，已通過。瀏覽器實際操作、獨立安全審查、SSO/RBAC、来源 SELECT、插件相容性、備份還原與負載測試留給相應交付階段，不以本輪 HTTP 測試代替。

## 診斷與安裝過程限制

- Python／shell primary LSP 未發現問題。YAML primary LSP 可識別已配置的 Compose tags，但 pi-lens auxiliary/cache 持續回報九個 `!reset`／`!override` 未解析警告。它們是 Docker Compose 官方有效 tags，已標記 false-positive；原生 `compose config`、merged-config 安全檢查及真啟動均通過。不將此描述為全專案診斷零錯誤。
- 首次映像下載在 600 秒時被前景命令 timeout 結束，保留已下載映像；確認無殘留 pull 程序後續傳，最終 exit 0。
- Compose `--dry-run up` 在模擬等待一次性工作時 timeout；確認實際尚無本案容器／資料卷。未拿 dry-run 作啟動證據；後續真 `up --wait` exit 0。
- Docker daemon 已啟用開機服務；常駐容器設 `unless-stopped`。僅實測本案 GMS／Frontend 重啟，**沒有重啟 WSL、Docker daemon 或整台主機**。

## 本機證據（不提交公開 repo）

`.local/evidence/`：`pull.log`、`pull-retry.log`／`.exit`、`cli-install.log`、`config-check.log`、`start-dry-run.log`、`start.log`、`live-check.log`、`restart.log`、`post-restart-check.log`、`runtime.json`。

`runtime.json` 只保存容器映像、狀態、埠與 restart policy，不含 Docker 環境變數。密碼、簽章金鑰與登入設定位於 gitignored `.local/`，不得上傳。

## 尚未交付

未接入業務資料庫、未執行 demo ingestion、未開發／載入自訂插件；未建立 Extension PostgreSQL、來源查詢 Broker、SQL 驗證功能。需要實際 SQL 執行／驗證的需求已保留，並非以 Context-only 取代。後續先確認來源與權限，再做 metadata-only 小範圍 ingestion。

本輪未 commit／push 新變更。
